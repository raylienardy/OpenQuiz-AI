# OpenQuiz AI - Backend

FastAPI backend for the OpenQuiz AI platform.

## Setup

1. Create a virtual environment:
   ```bash
   python -m venv venv
   source .venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows
   ```
